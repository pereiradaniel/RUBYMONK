#  Ruby uses the == operator for comparing to objects

puts true == true

#  The usual operators are also supported

puts 10 < 100
puts 50 >= 30

#  Boolean expressions always return either 'true' or 'false'


#  Combining Expressions using the && and || operators

puts age >= && (name == 'Bob' || name == 'Jill')

#  Negating expressions

name != Bob